# MGTSIM — Cesim-style static site template\n\nThis repo is a static, GitHub-Pages-ready template for MGTSIM.\n\n## Deploy\n1. Create a new GitHub repository (public)\n2. Upload all files or push via git\n3. In repository settings -> Pages -> Choose `main` branch and `/ (root)` folder\n4. Save and wait a minute; site will be live at `https://<username>.github.io/<repo>`\n\n## Customize\- Replace placeholders in `index.html` and `assets/css/style.css`\- Add your simulation folder (`/mgtsimulation/`)\- Update the Formspree endpoint if needed\